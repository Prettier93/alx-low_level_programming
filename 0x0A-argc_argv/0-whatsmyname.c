#include <stdio.h>

/**
 * main - prints the name of a program
 * @argc: argument count
 * @argc: argument vector 
 * Return: always 0
 */

int main(int argc_attributr_((unused)), char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
